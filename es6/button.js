"use strict";

import withStyle from "easy-with-style";  ///

import { Button } from "easy";

export default withStyle(Button)`

  font-size: inherit;
  font-family: inherit;
  font-weight: inherit;

`;
